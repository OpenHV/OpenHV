{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "OpenHV",
            "type": "mono",
            "request": "launch",
            "program": "${workspaceRoot}/engine/OpenRA.Game.exe",
            "cwd": "${workspaceRoot}",
            "args": ["Engine.LaunchPath=${workspaceRoot}", "Engine.ModSearchPaths=${workspaceRoot}/mods", "Game.Mod=hv"]
        },
    ]
}
