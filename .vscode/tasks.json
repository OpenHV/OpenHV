{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "msbuild",
            "args": ["-t:build", "-p:Configuration=Release", "-verbosity:m", "-nologo"],
            "windows": {
                "command": "dotnet",
                "args": ["build", "/p:Configuration=Release", "/nologo"]
            }
        }
    ]
}
